(library
  (name simpletree)
  (modules simpletree)
  (libraries core)
  (preprocess
  (pps ppx_deriving.show)))

(library
  (name simpledict)
  (modules simpledict)
  (libraries
    simpletree
    core)
  (preprocess
  (pps ppx_deriving.show)))
